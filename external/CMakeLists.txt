#--------------------------------------------------------------------
# soundplanelib/external/CMakeLists.txt
#--------------------------------------------------------------------

if(SP_USE_LIBUSB)
	add_subdirectory(libusb)
endif()
